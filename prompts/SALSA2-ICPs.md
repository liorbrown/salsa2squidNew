\# New PatchI want update squid route algo as follow\## GeneralThe new algo will send ICPs to all parents that SALSA2 selected, and forward to the one that hit if exist, otherwise send to the first MISS response\## New data member in Salsa2Proxy:peer winner;\## procedure when new request arrived:if (Config.Salsa2 \&\& Config.npeers) // This is the indication that this is squid proxy, config to salsa2. in the parents npeers == 0{&#x09;selectedPeers = Salsa2Proxy::SelectPeers();&#x09;if (!len(selectedPeers))&#x09;	route request to some parent, using round robin;&#x09;else&#x09;{&#x09;	this->winner = null;&#x09;	foreach(p in selectedPeers)&#x09;	{&#x09;		send ICP request (don't wait for one to response to send the next one, send them all)&#x09;	}&#x09;}}\-\## Procedure when ICP response returnsif (Config.Salsa2 \&\& Config.npeers) // This is the indication that this is squid proxy, config to salsa2. in the parents npeers == 0{&#x09;if (response == HIT)&#x09;	this->winner = current HIT cache&#x09;else if (!winner)&#x09;	this->winner = current MISS cache&#x09;if (response == HIT || (All ICP received))&#x09;	forward GET request to this->winner}\## Procedure when good HTTP response arrivemake sure that previous procedure disable to prevent double forwarding\## Procedure when some predefine timeout arrived before we got hit or before All ICP receivedroute request to some parent, using round robin;\## Notes1. for now, define the timeout hardcoded with the define macro, later we will insert it into the squid.config file2. for now, log any step of those procedures to log, using this function and format:*debugs(96,DBG\_CRITICAL,"Salsa2: {Insert your log msg here}");*